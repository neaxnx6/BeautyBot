# BeautyBot

Telegram-бот для записи на ногтевой сервис.

## Возможности

### Для Клиентов
- 💅 Запись на услуги через удобный интерфейс
- 📅 Двухуровневый выбор времени (день → время)
- 📝 Просмотр и отмена своих записей
- 🔔 Уведомления об изменениях от мастера
- 🛡️ Лимит 3 активных записи на клиента

### Для Мастера
- 📋 Управление расписанием
- 🗓️ Шаблоны рабочей недели
- ⚡ Быстрая генерация окошек на период
- 🗑️ Удаление дней и отдельных слотов
- ⏰ Настройка минимального времени для записи
- 👥 Просмотр записей клиентов

## Технологии

- **Python 3.10+**
- **aiogram 3.15** - фреймворк для Telegram Bot API
- **aiosqlite** - асинхронная работа с SQLite
- **python-dotenv** - управление переменными окружения

## Установка

```bash
# Клонировать репозиторий
git clone <repo-url>
cd BeautyBot

# Создать виртуальное окружение
python -m venv venv
source venv/bin/activate  # для Linux/Mac
# или
venv\\Scripts\\activate  # для Windows

# Установить зависимости
pip install -r requirements.txt

# Настроить переменные окружения
cp .env.example .env
# Отредактировать .env и добавить BOT_TOKEN и ADMIN_ID
```

## Конфигурация

Создайте файл `.env` в корне проекта:

```
BOT_TOKEN=your_bot_token_here
ADMIN_ID=your_telegram_id_here
```

## Запуск

```bash
python bot.py
```

## Деплой на Railway

1. Запушить код на GitHub
2. Создать новый проект на [railway.app](https://railway.app)
3. Подключить GitHub-репозиторий
4. Добавить переменные окружения в Railway:
   - `BOT_TOKEN`
   - `ADMIN_ID`
5. Railway автоматически запустит бота

## Структура Проекта

```
BeautyBot/
├── bot.py              # Точка входа
├── config.py           # Конфигурация
├── Procfile            # Для Railway
├── requirements.txt    # Зависимости
├── database/          # Работа с БД
│   ├── setup.py
│   ├── db_cmds.py
│   └── template_cmds.py
├── handlers/          # Обработчики бота
│   ├── start.py
│   ├── client.py
│   ├── master.py
│   └── template.py
├── keyboards/         # Клавиатуры
└── utils/            # Вспомогательные функции
```

## Roadmap (v2.0)

- 📅 Календарный вид для выбора дат
- 👥 База клиентов с контактами
- 📊 Статистика записей
- 🔧 Редактирование услуг через бота

## Лицензия

MIT
